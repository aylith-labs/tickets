# Tickets release media brief — 2026-09-25

Status: production brief only. This file does not announce a release or claim that paired films,
licensed narration, or posters exist. Published standalone binaries remain 0.1.3; the stable
browser ticket link is current source behavior, not a feature in those binaries.

## Source-backed story

“Capture it now. Find the exact ticket later.” Use a fresh disposable project and a local Tickets
daemon to create a short ticket in the browser, edit it, and open the stable-ID ticket link after
reload. The source-side link requires a running local daemon and the same store. It is not a hosted
share URL. Do not imply that creating a ticket already opens its detail; the list currently closes
the form and refreshes without selecting the new record. Avoid depicting an agent actually
working unless a separately authorized, configured terminal/harness run is captured.

Capture the same verified journey in light and dark at matched desktop and narrow viewport sizes:
empty state, create form, saved ticket detail, and stable link after reload. Record an actual
browser video for each theme with the exact source revision, local setup, route, and fixture data.
Use folder storage for a safe fixture but label it plainly: folder stores have no Git revision
history or undo. A Git-mode film needs its own Git 2.42+ setup and should show the separate store
branch accurately. The 0.1.3 npm archive cannot supply the full browser UI; do not use it as the
capture runtime or imply a published 0.1.4 exists.

Remotion motion design: one ticket card emerges from the author's note; a thin path connects the
local project ID and ticket ID to the browser URL; after a quiet reload the same record returns.
Use the actual browser frames as the center of the composition, with clear annotations for local
daemon, selected project, ticket ID, and saved status. Keep the action legible at changelog-card
size and allow reduced-motion viewing. The light and dark exports must each contain native theme
footage, not a recolored copy. Human-check captions against both picture and final voice timing.

Narrate with a licensed Cartesia voice chosen for Tickets. Draft script: “Write the task where it
occurs. Tickets saves it in your local store and gives the record a stable project and ticket ID.
Open that link after a reload, and you return to the same work. Hand-off to a coding agent is
available when your own terminal and agent are configured.” Confirm every word against recorded
behavior before the provider call; no voice selection, credential use, or audio file is claimed.

Make paired exceptional posters from a real ticket-detail capture and a crisp local-link callout,
using Aylith's warm stone/orange system. Title the poster “The task has a place to return to.”
Avoid a fabricated agent terminal or false Git-undo badge. Verify the image at thumbnail and
full-width sizes. The existing central changelog has one old still and no paired video, narration,
captions, or posters. All new assets, media provenance, publication, and live readback remain open.

## Product and release gates

The next high-value behavior is create → open the created ticket automatically, followed by its
stable link. Implement and test it at the agreed browser UI seam after Steven's `/tdd` seam answer.
Keep source-only behavior separate from 0.1.3 package claims. Do not expose the auth-less daemon
to untrusted networks or treat media publishing as available without configured publisher access.
